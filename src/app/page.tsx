import {
  HeroSection,
  AboutSection,
  ServicesSection,
  OffersSection,
  ContactSection,
} from "@/components";

const Home = () => {
  return (
    <main>
      <HeroSection />
      <AboutSection />
      <ServicesSection />
      <OffersSection />
      <ContactSection />
    </main>
  );
};

export default Home;
